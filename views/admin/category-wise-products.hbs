<div class="container">
    <div class="row d-flex justify-content-center">
        <div class="m-5" style="background: #fff;">
            <h2 class="text-center fw-bold text-decoration-underline">Category wise Products- <span style="color: #0448f5e8;">{{products.0.Category}}</span> </h2>
            {{#if products}}
                <table class="table table table-striped table-bordered" id="example" id="catProducts">
                <thead>
                    <tr>
                        <th scope="col">Product Name</th>
                        <th scope="col">Product_Details</th>
                        <th scope="col" class="text-center">Quantity</th>
                        <th scope="col">Price</th>
                        <th scope="col">Description</th>
                        <th scope="col">Image</th>
                        <th scope="col" class="text-center">Option</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each products}}
                    <tr>
                        <td>{{this.Name}}</td>
                        <td>{{this.Category}}</td>
                        <td class="text-center">{{this.quantity}}</td>
                        <td>{{this.Price}}</td>
                        <td>{{this.Description}}</td>
                        <td><img src="/product-images/{{this._id}}.jpg" alt="" style="width:100px; height: 80px;"> </td>
                        <td class="text-center" width="20%">
                            <a href="/edit?id={{this._id}}" class="btn btn-primary"><i class="fas fa-edit"></i></a>
                            <a href="/delete?id={{this._id}}" class="btn btn-danger"
                                onclick="return confirm('Are you want to delete {{this.Name}} ?')"><i class="fas fa-trash-alt"></i></a>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            {{else}}
                <h4 class="text-center mt-5 text-danger">Data Unavailable!</h4>
            {{/if}}
            
        </div>
    </div>
    
    {{!-- <script>
        function viewCategory(catName){
            alert("Function")
            $.ajax({
                url:'/product-category-view',
                data:{
                    name:catName
                },
                method:'post',
                success:(response)=>{
                    if(response.getCategoryProduct)
                    {
                        console.log(response.products)
                        alert("Product Get")
                        document.getElementById('catProducts').value=response.products
                    }
                    else{
                        alert("something Error")
                    }
                }
            })
        }
    </script> --}}


    <script type="text/javascript" charset="utf8"
        src="https://cdn.datatables.net/1.12.1/js/jquery.dataTables.min.js"></script>
    <script type="text/javascript" charset="utf8"
        src="https://cdn.datatables.net/1.12.1/js/dataTables.bootstrap4.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#example').DataTable();
        });
    </script>